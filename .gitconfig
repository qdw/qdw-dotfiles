[user]
	name  = Quinn Weaver
	email = quinn@pgexperts.com

[alias]
	co = checkout
        fc = diff --name-only
        stat = status
        tree = git log --graph --decorate --pretty=oneline --abbrev-commit

[core]
	excludesfile = ~/.gitignore

[diff]
	color = auto
	tool  = git diff --patience
